﻿将 MsQuic 的回调或操作“外包”到一个异步方法层（即在 MsQuic 外面再套一层异步逻辑），是高性能、高可靠系统设计中的关键实践。
这样做不是多余的，而是出于以下几个深刻的技术原因：
核心原因总结
MsQuic 线程是“黄金线程”——它负责整个 QUIC 协议栈的高效运转。任何阻塞或耗时操作都会影响所有连接的性能。
因此，必须尽快“逃离”MsQuic 线程，把用户逻辑交给 .NET 的线程池或异步调度器来处理。